apiVersion: hauler.cattle.io/v1alpha1
kind: Content
metadata:
  name: mycontent
spec:
  files:
    - name: myfile
      blobs:
        - ref: "file://testdata/content-a.yaml"
        - ref: "https://get.k3s.io"

  images:
    - ref: alpine:latest